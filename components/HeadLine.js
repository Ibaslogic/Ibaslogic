const HeadLine = ({ title }) => {
  return (
    <h2 className="text-2xl sm:text-2xl lg:text-[1.65rem] text-[#1a2c47] font-semibold tracking-[-.07rem] !leading-[1.2]">
      {title}
    </h2>
  );
};

export default HeadLine;
