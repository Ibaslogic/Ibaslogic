const FooterTitle = ({ title }) => {
  return (
    <h4 className="text-black mb-[15px] font-semibold text-[0.95em] tracking-tighter capitalize">
      {title}
    </h4>
  );
};

export default FooterTitle;
