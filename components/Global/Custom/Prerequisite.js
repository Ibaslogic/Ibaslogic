import { BiLink } from 'react-icons/bi';

const Prerequisite = () => {
  return (
    <h2 id="prerequisite">
      Prerequisite
      <a className="anchor" href="#prerequisite">
        <BiLink />
      </a>
    </h2>
  );
};

export default Prerequisite;
